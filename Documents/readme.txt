Author: Marcus Andrews (marcus.k.s.andrews@gmail.com)

1. Place the three csv files in your desired location.  The program defaults to looking in C://temp and looking for a csv called mid-test.csv
2. Run the Program by calling the main method with an optional argument detailing the filepath to the target csv (e.g. "C://marcusSubmission/mid-test.csv")
3. There is minimal error handling as this is a test project.
4. The author is not completely comfortable writing a switch statement to identify masks by issuer name, as it is brittle (E.g. The pitfall of double space in the supplied test file mid-test.csv).
5.  

